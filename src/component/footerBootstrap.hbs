<section>
    <nav>
        <div class="col-10">
            <div class="row gx-3">
                {{#each footer}}
                <div class="col-2">
                    <div class="row gy-2">
                        {{#each this}}
                        <a href="{{link}}" class="btn btn-link {{#if @last}}disabled{{/if}} text-start">{{text}}</a>
                        {{/each}}
                    </div>
                </div>
                {{/each}}
            </div>
        </div>
    </nav>
</section>